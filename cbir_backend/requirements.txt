# -----------------------------
# 🧠 VisionFind CBIR Requirements
# -----------------------------

# Django stack
Django==5.0.1
djangorestframework==3.14.0
djangorestframework-simplejwt==5.3.0
django-cors-headers==4.3.1
drf-yasg==1.21.7

# Machine Learning / Feature Extraction
# ✅ GPU-enabled PyTorch build will be installed separately (see note below)
torch>=2.0.0
torchvision>=0.15.0
git+https://github.com/openai/CLIP.git

# ✅ FAISS - CPU version only (GPU build not available for Windows)
faiss-cpu>=1.7.4

# Core dependencies
Pillow>=10.0.0
numpy>=1.24.0
python-dotenv==1.0.0

# Production / Deployment
gunicorn==21.2.0

# -----------------------------
# ⚙️ Notes:
# 1️⃣  On Windows, use:
#       pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu130
#     This gives you CUDA 13.0 support for your RTX GPU.
#
# 2️⃣  faiss-gpu is not available on Windows.
#     Keep faiss-cpu installed — CLIP still runs on GPU (the heavy part).
#
# 3️⃣  If you ever switch to Linux/WSL for deployment,
#     you can replace "faiss-cpu" with "faiss-gpu".
# -----------------------------
